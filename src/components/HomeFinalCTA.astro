<section class="final" aria-label="Appel à l’action">
  <div class="wrap">
    <div class="final-inner">
      <div>
        <div class="k">CONTACT</div>
        <h2 class="t">Un besoin critique ? On cadre et on livre.</h2>
        <p class="p">Décris ton contexte. Réponse rapide, cadrage clair, exécution gouvernée.</p>
      </div>
      <div class="cta">
        <a class="btn btn-primary" href="/contact">Parler à un expert →</a>
        <a class="btn btn-secondary" href="/services">Voir les services →</a>
      </div>
    </div>
  </div>

  <style>
    .final{ background:#fff; border-top: 1px solid rgba(var(--line),1); padding: 52px 0; }
    .final-inner{
      border:1px solid rgba(var(--line),1);
      background: rgba(0,0,0,0.012);
      border-radius: 18px;
      padding: 20px 18px;
      display:flex; justify-content:space-between; align-items:flex-end; gap: 18px;
    }
    .k{ font-size:12px; font-weight:900; letter-spacing:.18em; text-transform:uppercase; color: rgba(var(--muted),0.92); }
    .t{ margin: 10px 0 0; font-size: 30px; line-height:1.12; letter-spacing:-0.02em; font-family: ui-serif, Georgia, "Times New Roman", Times, serif; }
    .p{ margin: 10px 0 0; max-width: 70ch; color: rgba(var(--ink),0.78); line-height:1.85; font-size: 16px; }
    .cta{ display:flex; gap: 12px; flex-wrap: wrap; }
    @media (max-width: 980px){ .final-inner{ flex-direction:column; align-items:flex-start; } }
  </style>
</section>