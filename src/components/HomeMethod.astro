<section class="home-block" aria-label="Méthode - aperçu">
  <div class="wrap">
    <div class="head">
      <div>
        <div class="k">MÉTHODE</div>
        <h2 class="t">Cadrer. Sécuriser. Livrer. Prouver.</h2>
        <p class="p">Une exécution lisible : décisions explicites, contrôles, incréments livrables.</p>
      </div>
      <div class="cta">
        <a class="btn btn-secondary" href="/methode">Voir la méthode →</a>
      </div>
    </div>

    <div class="steps">
      <div class="step"><div class="n">01</div><div class="s">Cadrage</div><div class="d">Objectifs, périmètre, dépendances, critères d’acceptation.</div></div>
      <div class="step"><div class="n">02</div><div class="s">Sécurisation</div><div class="d">Sécurité, conformité, architecture cible, readiness.</div></div>
      <div class="step"><div class="n">03</div><div class="s">Exécution</div><div class="d">Build, tests, intégration, déploiement, stabilisation.</div></div>
      <div class="step"><div class="n">04</div><div class="s">Preuves</div><div class="d">Traçabilité, métriques, livrables, documentation exploitable.</div></div>
    </div>
  </div>

  <style>
    .steps{ margin-top: 18px; display:grid; grid-template-columns: repeat(4, minmax(0,1fr)); gap: 14px; }
    /* QA polish: micro-cards (bg-gray-50 equivalent) + laser index */
    .step{ border:1px solid rgba(var(--line),1); background: #fbfbfc; padding: 16px; border-radius: 16px; }
    .n{ font-weight: 950; letter-spacing:-0.01em; color: rgba(var(--red),1); }
    .s{ margin-top: 8px; font-weight: 950; letter-spacing:-0.01em; }
    .d{ margin-top: 8px; color: rgba(var(--ink),0.82); line-height:1.75; font-size: 14px; }
    @media (max-width: 980px){ .steps{ grid-template-columns: 1fr 1fr; } }
    @media (max-width: 560px){ .steps{ grid-template-columns: 1fr; } }
  </style>
</section>