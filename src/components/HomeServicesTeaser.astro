<section class="home-block" aria-label="Services - aperçu">
  <div class="wrap">
    <div class="head">
      <div>
        <div class="k">SERVICES</div>
        <h2 class="t">Un delivery conçu pour livrer, avec contrôle.</h2>
        <p class="p">
          Cadrage, intégration, automatisation et IA, avec sécurité, conformité et qualité intégrées dès le départ.
        </p>
      </div>
      <div class="cta">
        <a class="btn btn-secondary" href="/services">Voir les services</a>
      </div>
    </div>

    <div class="grid">
      <article class="cardx">
        <h3>Delivery TI & intégration</h3>
        <p>Architecture, CI/CD, environnements, industrialisation et observabilité, pour livrer vite et proprement.</p>
      </article>
      <article class="cardx">
        <h3>Renfort & équipes</h3>
        <p>Profils ciblés ou équipe dédiée, responsabilités claires, priorisation et qualité.</p>
      </article>
      <article class="cardx">
        <h3>IA & automatisation</h3>
        <p>Solutions IA utiles, intégrées aux outils existants, avec garde-fous et traçabilité.</p>
      </article>
    </div>
  </div>

  <style>
    .home-block{ background:#fff; padding: 52px 0; }
    .head{
      display:flex; justify-content:space-between; gap: 18px; align-items:flex-end;
      border-bottom: 1px solid rgba(var(--line),1); padding-bottom: 18px;
    }
    .k{ font-size:12px; font-weight:900; letter-spacing:.18em; text-transform:uppercase; color: rgba(var(--muted),0.92); }
    .t{ margin: 10px 0 0; font-size: 34px; line-height:1.1; letter-spacing:-0.02em; font-family: ui-serif, Georgia, "Times New Roman", Times, serif; }
    .p{ margin: 10px 0 0; max-width: 72ch; color: rgba(var(--ink),0.78); line-height:1.85; font-size: 16px; }
    .grid{ margin-top: 18px; display:grid; grid-template-columns: repeat(3, minmax(0,1fr)); gap: 14px; }
    .cardx{ border:1px solid rgba(var(--line),1); background: rgba(0,0,0,0.012); padding: 18px 16px; border-radius: 16px; }
    .cardx h3{ margin:0; font-weight:950; letter-spacing:-0.01em; font-size: 18px; }
    .cardx p{ margin: 10px 0 0; color: rgba(var(--ink),0.75); line-height:1.75; font-size: 14px; }
    @media (max-width: 980px){
      .head{ flex-direction:column; align-items:flex-start; }
      .grid{ grid-template-columns: 1fr; }
    }
  </style>
</section>
