---
const { href = '#', class: className = '', ariaLabel, target, rel } = Astro.props;
---
<a href={href} class={`s1-local ${className}`.trim()} aria-label={ariaLabel} target={target} rel={rel}><slot /></a>
